/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package cs542_project;

import java.awt.Color;

/**
 *
 * @author Jack and Duran
 */
public class Cs542_project {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        GameManager test = new GameManager();
        
        
        /*
        Army a_test = new Army(50);
        Cavalry cman = new Cavalry(a_test);
        a_test.addUnit(cman);
        
        
        GameGUI tester = new GameGUI(null);
        tester.showBoard(12);
        tester.setVisible(true);
        
        tester.getBoard().highlightLegalStartingPositions(a_test.getArmyColor());
        
        tester.getBoard().getSquare(new int[]{2,2}).setOccupier(cman);
        tester.getBoard().getSquare(new int[]{2,2}).redrawIcons();
        */
    }
    
}
